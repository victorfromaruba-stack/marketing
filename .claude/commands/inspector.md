---
description: Run the inspector on a prospect
---

Use the **inspector** subagent: $ARGUMENTS.

Its full brief is in `.claude/agents/inspector.md` — follow it exactly, including the read-first
and write-after steps. The org chart and handoffs are in `ORG.md`.

Before you start: read `memory/playbook.md`. If it is empty, say so plainly and note that you
are working from reasoned hypotheses rather than evidence.
